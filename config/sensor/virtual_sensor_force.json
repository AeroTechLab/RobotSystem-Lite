{
  "inputs": [
    { "interface": { "type": "DummyIO", "config": "device_0", "channel": 2 } },
    { "interface": { "type": "DummyIO", "config": "device_1", "channel": 2 } }
  ],
  "output": "( in0 - in1 ) * 10.0 / 3.0"
}
